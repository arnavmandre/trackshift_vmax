# Instructions for this event workspace

Read README.md and EVENT_WORK.md. The user explicitly requested reuse of the
simulator and simulation clips. That authorization supersedes the earlier
blanket restriction for those specific resources; it is not organiser approval.

Keep their provenance disclosed. Do not present reused simulation code or data
as newly built. Do not reimport the old VMAX model, weights, UI or inference code.
The new experiment fine-tunes an external pretrained pose model; credit it.

Never conceal history, rename prior work to imply new authorship, or fabricate
organiser permission. Record actual experiment results and limitations.
Do not tune model/settings against the fresh final test results. Once inspected,
that set becomes regression data and subsequent final testing needs a new seed.
