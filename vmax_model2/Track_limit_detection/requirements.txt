# Dependencies for train_mask2former_swin_tiny.ipynb
# Install inside your existing machine-learning conda environment.
# For GPU training, torch and torchvision must be CUDA-enabled builds.
torch>=2.6,<3
torchvision>=0.21,<1
transformers==4.57.6
timm>=1.0,<2
numpy<3
scipy
pillow
opencv-python-headless
matplotlib
tqdm
ipykernel
jupyterlab

# Optional simulator rendering (needed by spam/vmax/generate_extra.py).
moderngl==5.12.0
glcontext==3.0.0
