# Deep model environment (.venv_bench): vmax_bridge.py and its tests only.
# Must stay separate from the main .venv (different PyTorch version).
# PyTorch is installed first by setup.ps1 / setup.sh, which picks the CUDA or CPU
# build for your machine. requirements.txt (training notebook) is separate.
transformers==4.57.6
huggingface-hub==0.36.2
safetensors==0.8.0
timm==1.0.29
numpy==2.5.2
scipy==1.18.1
pillow==12.3.0
opencv-python-headless==5.0.0.93
tqdm==4.70.1
